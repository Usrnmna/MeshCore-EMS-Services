LINUX: START THE CONTINUOUS SERVICE

Run this one command from the service folder:

    bash Start-Service.sh

It sets up the local Python environment if missing, starts the local MQTT broker,
and opens the MeshCore -S device selector. Select the USB client node once; the
service then keeps listening and responding on #autatestbot until Ctrl+C.

Keep its terminal open. Run only one instance for the USB device.
This does not install an automatic boot service or bypass the -S selector.

If your file copy preserves executable permission, you may also run:

    ./Start-Service.sh

Otherwise bash Start-Service.sh works without changing file permissions.
No existing configuration or program files were changed to add this launcher.

AQI: Set AIRNOW_API_KEY in the launching environment (see README), then send
!aqi LATITUDE LONGITUDE or !aqi LATITUDE, LONGITUDE on #autatestbot.

TRAFFIC: Send !traffic 80 (or another route number 1-999) on #autatestbot.
No API key needed. Restart the service to load the new command.

RIVERS: Send !rivers LATITUDE LONGITUDE (or LATITUDE, LONGITUDE) on #autatestbot.
Searches within 15 miles. No API key needed. Restart to load the new command.
